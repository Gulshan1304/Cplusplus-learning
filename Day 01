#include<iostream>
using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}
// This is a simple C++ program that prints "Hello, World!" to the console.
// It includes the iostream library for input and output operations, uses the standard namespace to avoid prefixing standard library names with "std::", and defines the main function which is the entry point of the program.
// The program returns 0 to indicate that it finished successfully.
