# 1. Structure (struct)
A structure is used to group different types of variables under one name.
Syntax:
struct Student {
    string name;
    int age;
    float marks;
};

# 2. Enumeration (enum)

An enum is a user-defined type that assigns names to integral constants.

Instead of writing numbers like 0, 1, 2, you can use meaningful names.